<script setup lang="ts">
    const mode = useColorMode()
    const props = withDefaults(defineProps<{ fill?: string, mini?: boolean }>(), { fill: 'var(--ui-primary)'})
</script>

<template>

</template>